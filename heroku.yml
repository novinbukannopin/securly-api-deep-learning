setup:
  addons:
    - plan: heroku-redis
      as: REDIS
build:
  docker:
    api: Dockerfile
release:
  command:
    - echo "Release phase tasks can be defined here, if needed."
run:
  api: python app.py
  redis:
    image: redis:alpine
